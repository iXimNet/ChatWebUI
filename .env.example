# OpenAI兼容API的基础URL (必须)
# 示例: https://api.openai.com/v1 或 https://your-domain.com/v1
API_BASE_URL=your_api_base_url_here

# API认证密钥 (必须)
API_KEY=your_api_key_here

# 使用的模型名称 (可选，默认gpt-3.5-turbo)
# 可选值: gpt-3.5-turbo, gpt-4等
MODEL_NAME=gpt-3.5-turbo

# 系统提示词 (可选)
# {{date}}会被自动替换为当前日期
API_SYSTEM_PROMPT="You are a helpful assistant. Today's date is {{date}}."

# 服务运行端口 (可选，默认3000)
PORT=3000

# 是否记录API原始响应 (可选，默认false)
# 设置为true时会在api_response.log中记录原始数据
API_LOG=false
