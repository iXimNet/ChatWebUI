# 服务运行端口 (可选，默认3000)
PORT=3000

# Admin Password Hash (do not set manually if using the setup interface)
# This will be populated by the application during the initial admin setup.
ADMIN_PASSWORD_HASH=

# A strong, random string used to sign the session ID cookie.
# Generate a secure secret for production environments.
# Example: openssl rand -base64 32
SESSION_SECRET=your_strong_session_secret_here

# Other application configurations (like API_BASE_URL, API_KEY, MODEL_NAME, API_SYSTEM_PROMPT, API_LOG)
# will be managed via the admin interface and stored in config.json once admin setup is complete.
